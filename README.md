# waeditor

泰坦重铸时光服武器天赋战士 WA 字符串编辑器（HTML + JS + CSS）。

## 功能

- 配置怒气高/低阈值语音提示（技能1/技能2）
- 配置目标缺少 DOT（默认撕裂）语音提示
- 对比冲锋与拦截剩余冷却并提示更快技能
- 基于撕裂剩余时间与触发周期预测无条件压制触发时间
- 生成/载入可编辑的 WA 字符串（`WAEDITOR1:` + Base64 JSON）

## 使用

直接在浏览器打开 `/home/runner/work/waeditor/waeditor/index.html`。
